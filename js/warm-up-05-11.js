firstObject('tacocat') {
    string: 'tacocat',
        size: 7.
    containsLetter: true,
        isOneWord: true
}

function makeObjectFromString(str){
    const =obj = {};
    obj.string = str;
    obj.size =str.length;
    obj.isOneWord =str.indexOf("") <= 0;
    let lc = str
}